name: Lint

on:
  workflow_run:
    workflows: ["Build"]
    types: 
      - completed
    
jobs:

  lint:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    name: Lint
    runs-on: self-hosted
    needs: [build]

    steps:
      - run: yarn lint
