name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:

  prepare: 
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [16.x]

    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'yarn'
      - run: yarn install --frozen-lockfile

  build:
    needs: prepare
    steps:
      - run: yarn game ci:build

  format:
    needs: prepare
    steps:
      - run: yarn format:check

  lint:
    needs: prepare
    steps:
      - run: yarn lint

  prune:
    needs: prepare
    steps:
      - run: yarn prune-imports
